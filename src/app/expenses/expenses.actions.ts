import { createAction, props } from '@ngrx/store';

export const addIncomeOutcome = createAction('[Expenses Module] addIncomeOutcome');
export const getAllIncomeOutCome = createAction('[Expenses Module] getAllIncomeOutCome');